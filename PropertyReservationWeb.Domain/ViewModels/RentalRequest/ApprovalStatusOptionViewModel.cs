using PropertyReservationWeb.Domain.Enum;

namespace PropertyReservationWeb.Domain.ViewModels.RentalRequest
{
    public record ApprovalStatusOptionViewModel(ApprovalStatus Value, string DisplayName);
}
